apiVersion: v1
kind: Service
metadata:
  name: spring-service
spec:
  selector:
    app: spring
  ports:
    - protocol: TCP
      port: 8080  # Spring 애플리케이션이 동작할 포트
      targetPort: 8080
  type: ClusterIP
