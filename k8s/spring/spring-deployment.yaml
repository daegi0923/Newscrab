apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring
  template:
    metadata:
      labels:
        app: spring
    spec:
      containers:
        - name: spring
          image: your-docker-hub-account/spring-app:latest  # Spring 애플리케이션의 Docker 이미지
          ports:
            - containerPort: 8080
