apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
spec:
  capacity:
    storage: 1Gi  # MySQL에 필요한 스토리지 크기 설정
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/mysql"  # 클러스터 노드의 실제 경로
